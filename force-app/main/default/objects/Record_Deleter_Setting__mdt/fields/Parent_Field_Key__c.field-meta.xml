<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Parent_Field_Key__c</fullName>
    <description>Key for which field on the parent object, the child object's field's value must match (e.g. `$.Id` (which would be the parent object's record id)).</description>
    <externalId>false</externalId>
    <fieldManageability>DeveloperControlled</fieldManageability>
    <inlineHelpText>Key for which field on the parent object, the child object's field's value must match (e.g. `$.Id` (which would be the parent object's record id)).</inlineHelpText>
    <label>Parent Field Key</label>
    <length>255</length>
    <required>true</required>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
