<?xml version="1.0" encoding="utf-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Tag_Icon_URL__c</fullName>
    <businessStatus>Active</businessStatus>
    <description>This formula field that provides the URL so that the icon/image can be displayed on the tag inside the UI/LWC. Uses Tag_Api_Name__c within the formula and maps to the image stored in static resources.</description>
    <externalId>false</externalId>
    <formula>$CustomMetadata.Tag_Icon_URL__mdt.Tag_Icon.Tag_Icon_URL__c  +  Tag_Api_Name__c</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <inlineHelpText>This formula field that provides the URL so that the icon/image can be displayed on the tag inside the UI/LWC. Uses Tag_Api_Name__c within the formula and maps to the image stored in static resources.</inlineHelpText>
    <label>Tag_Icon</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
