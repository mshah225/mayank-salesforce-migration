<apex:component id="LiveAgentNotices" controller="LiveAgentNoticesController">
    <apex:slds />
    <apex:repeat value="{!noticeList}" var="notice">
        <div style="padding: 2px;">
            <div class="slds-notify_container slds-is-relative">
                <div class="slds-notify slds-notify_toast slds-theme_info" style="float: left; background-color: #8C1D40 !important; width: 75% !important; min-width:50px !important;" role="status">
                    <span class="slds-assistive-text">info</span>
                    <div class="slds-notify__content">
                        <apex:repeat value="{!notice}" var="noticeLine">
                            <ul style="list-style-type:none">
                                <li style="padding: 5px 0px;">{!noticeLine}</li>
                            </ul>
                        </apex:repeat>
                    </div>
                </div>
            </div>
        </div>
        <div style="clear:both"/>
    </apex:repeat>
    <style>
    </style>
</apex:component>