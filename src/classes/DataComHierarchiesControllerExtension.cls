public class DataComHierarchiesControllerExtension {
    public DataComHierarchiesControllerExtension(ApexPages.StandardController controller) {
        // This class was originally designed to extend a managed package. The managed
        // package was uninstalled. We cannot delete this class in prod so we are simply
        // deleting it's contents.
    }
}
