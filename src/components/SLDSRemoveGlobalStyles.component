<apex:component>
    <!-- 1. Insert component immediately after <apex:slds /> tag -->
    <!-- 2. Add 'slds-scope' class to desired SLDS-based container -->
    <script>
        document.body.className = document.body.className.replace("slds-scope", "");
    </script>
</apex:component>